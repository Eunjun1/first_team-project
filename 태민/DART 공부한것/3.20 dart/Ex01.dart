
main(){
  int inputValue = 4;
  int Factorial = 1;
  for (int i = Factorial; i <= inputValue; i++){
    Factorial = Factorial * i;
  }
    print(Factorial);
    // i 에 1을 더한다
    // 기존에 있던 factorial값에 다음 i를 곱한다.
}